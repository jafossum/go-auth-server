syntax = "proto3";
package auth;

message Authorization {
    string api_key = 1;
    string api_key_value = 2;
    repeated Client clients = 3;
}

message Client {
    string client_id = 1;
    string client_secret = 2;
    string is_admin = 3;
    repeated string scope = 4;
}